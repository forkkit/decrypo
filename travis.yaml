language: go
os:
  - osx
  - windows

branches:
  only:
    - master

script:
  - export TIME=`date +%FT%T%z`
  - export CGO_ENABLED=1
  - go build -ldflags "-s -w -X github.com/ajdnik/decrypo/build.version=0.2.3 -X github.com/ajdnik/decrypo/build.datetime=${TIME}" -o decrypo
